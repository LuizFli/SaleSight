# Prisma database connection string
# Se o banco roda fora do compose e você acessa a partir do container:
#   - Windows/Mac (Docker Desktop): use host.docker.internal
#   - Linux: coloque o IP da máquina host ou do servidor MySQL
DB_HOST=host.docker.internal
DATABASE_URL="mysql://root:senai@${DB_HOST}:3306/salesigth_db"

# JWT secrets (dev-safe defaults; change in prod)
ACCESS_TOKEN_SECRET="dev_access_secret"
REFRESH_TOKEN_SECRET="dev_refresh_secret"

# Token TTLs (jsonwebtoken formats supported)
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="8h"
